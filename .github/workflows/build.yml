name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      board: "nice_nano_v2"
      shield: "my5x6"          # <-- matches your shield folder name
      # If you want multiple builds (e.g., left/right for split), add more lines here
      # Example for extra config:
      # config: "path/to/extra.conf"  # optional

  # Optional: artifact upload step if needed
